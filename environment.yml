name: wakeagent
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy
  - scipy
  - pytest
  - pydantic
  - pip:
      - sounddevice
      - openwakeword
      - onnxruntime
      - faster-whisper
      - silero-vad
